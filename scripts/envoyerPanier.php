<?php
/*
// Démarre la session
session_start();

// Vérifie si la session Panier existe
if(isset($_SESSION['Panier'])) {
    // Récupère le Panier depuis la session
    $panier = $_SESSION['Panier'];

    // Encode le Panier en JSON
    $panierJson = json_encode($panier);

    // Renvoie le Panier JSON en réponse à la requête AJAX
    header('Content-Type: application/json');
    echo $panierJson;
} else {
    // Si la session Panier n'existe pas, renvoie un message d'erreur
    header('HTTP/1.1 404 Not Found');
    echo json_encode(array('message' => 'Session Panier introuvable'));
}*/
?>
